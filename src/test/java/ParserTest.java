import org.junit.Test;

public class ParserTest {

//    @Test
//    public void testValid() throws Exception {
//        Parser parser = new Parser(null, db);
////        assertFalse(parser.valid(Record.builder().build()));
////        assertTrue(parser.valid(Record.builder()
////                .propertyId("asd")
////                .cityName("1")
////                .build()));
//    }
}
